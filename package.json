{
  "private": true,
  "name": "boba",
  "version": "0.0.0",
  "scripts": {
    "debug": "dlv debug --headless --api-version=2 --listen=127.0.0.1:43000",
    "version": "changeset version && pnpm install",
    "publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.11"
  }
}
